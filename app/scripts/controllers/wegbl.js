'use strict';

/**
 * @ngdoc function
 * @name sflowApp.controller:WegblCtrl
 * @description
 * # WegblCtrl
 * Controller of the sflowApp
 */
angular.module('sflowApp')
  .controller('WegblCtrl', function ($scope) {
    $scope.ttp = 0;
    $scope.bw = false;
  });
